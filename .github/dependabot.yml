version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # uv ecosystem disabled: Dependabot bundles an older uv that cannot resolve
  # our conflicts + environments setup. Replaced by .github/workflows/update-deps.yml.
