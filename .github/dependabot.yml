version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # Pinned intentionally — update manually to stay aligned with HA releases.
      - dependency-name: "pytest-homeassistant-custom-component"
